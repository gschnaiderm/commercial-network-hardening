filter {
  if [type] == "syslog" {
    grok {
      match => { "message" => "%{SYSLOGTIMESTAMP:syslog_timestamp} %{NOTSPACE:syslog_hostname} %{DATA:syslog_program}(?:\[%{POSINT:syslog_pid}\])?: %{GREEDYDATA:syslog_message}" }
      add_field => [ "received_at", "%{@timestamp}" ]
      add_field => [ "received_from", "%{host}" ]
    }
    
    # If it's Suricata eve.json embedded in syslog
    if [syslog_program] == "suricata" {
      json {
        source => "syslog_message"
        target => "suricata"
        remove_field => [ "syslog_message" ]
      }
      
      # Optional: Extract timestamp from the JSON if present
      if [suricata][timestamp] {
        date {
          match => [ "[suricata][timestamp]", "ISO8601" ]
        }
      }
    }
  }
}
